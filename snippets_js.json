{
    "js標準入力": {
        "prefix": "js_input",
        "body": [
          "function Main(input) {",
          "  inp = input.split(\"\\n\")",
          "  // 整数の入力",
          "  let N = parseInt(inp[0]);",
          "  // リストの入力",
          "  let L = inp[1].split(\" \").map(n => parseInt(n));",
          "  // 二次元配列の入力",
          "  let C = inp[2].map((n) => n.split(' ').map((n) => parseInt(n)));",
          "};",
          "",
          "Main(require(\"fs\").readFileSync(\"/dev/stdin\", \"utf8\"));"
        ],
        "description": "js標準入力"
    },

    "ソート": {
        "prefix": "sort_js",
        "body": [
          "// 配列のソート",
          "let arr = [1, 3, 2, 5, 4];",
          "// 昇順",
          "arr = arr.sort((a, b) => a - b);",
          "// 降順",
          "arr = arr.sort((a, b) => b - a);"
        ],
        "description": "ソート"
    }
}